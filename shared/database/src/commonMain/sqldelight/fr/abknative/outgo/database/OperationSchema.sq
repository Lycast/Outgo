CREATE TABLE OperationEntity (
    id TEXT NOT NULL PRIMARY KEY,
    userId TEXT NOT NULL,
    walletId TEXT NOT NULL,
    name TEXT NOT NULL,
    amountInCents INTEGER NOT NULL,
    type TEXT NOT NULL,
    recurrence TEXT NOT NULL,

    -- Moteur temporel
    startDate INTEGER NOT NULL,
    endDate INTEGER,

    -- Métadonnées
    createdAt INTEGER NOT NULL,
    updatedAt INTEGER NOT NULL,
    deletedAt INTEGER,
    syncStatus TEXT NOT NULL,

    FOREIGN KEY (walletId) REFERENCES WalletEntity(id) ON DELETE CASCADE
);

CREATE INDEX idx_operation_active_period ON OperationEntity(userId, walletId, deletedAt, startDate);
CREATE INDEX idx_operation_sync ON OperationEntity(userId, syncStatus);