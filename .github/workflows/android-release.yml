name: Android Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Decode Keystore
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_FILE }}
        run: echo "$KEYSTORE_B64" | base64 -di > outgo-upload-key.jks

      - name: Decode Google Services JSON
        env:
          GOOGLE_SERVICES_B64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: echo "$GOOGLE_SERVICES_B64" | base64 -di > android/app/google-services.json

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      # Une seule étape de build suffit pour générer les deux formats (AAB et APK)
      - name: Build and Sign Release Bundle and APK
        run: ./gradlew :android:app:bundleRelease :android:app:assembleRelease
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          WEB_CLIENT_ID: ${{ secrets.WEB_CLIENT_ID }}
          BASE_URL: ${{ secrets.BASE_URL }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: outgo-release-artifacts
          path: |
            android/app/build/outputs/bundle/release/*.aab
            android/app/build/outputs/apk/release/*.apk